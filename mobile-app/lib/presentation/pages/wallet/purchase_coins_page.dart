import 'package:flutter/material.dart';

class PurchaseCoinsPage extends StatelessWidget {
  const PurchaseCoinsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Purchase Coins')),
      body: const Center(child: Text('Purchase Coins')),
    );
  }
}

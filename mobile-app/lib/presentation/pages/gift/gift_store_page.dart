import 'package:flutter/material.dart';

class GiftStorePage extends StatelessWidget {
  const GiftStorePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Gift Store')),
      body: const Center(child: Text('Gift Store')),
    );
  }
}
